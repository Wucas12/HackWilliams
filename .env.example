# Copy to .env.local and fill in values for local development.
# For production (e.g. Vercel), set these in your host's environment.

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Local: use http://localhost:3000/api/auth/google/callback
# Production (saturday.solutions): use https://saturday.solutions/api/auth/google/callback
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

OPENAI_API_KEY=
# Optional
TAVILY_API_KEY=
# Optional; app does not use NextAuth but some setups reference it
NEXTAUTH_URL=http://localhost:3000
